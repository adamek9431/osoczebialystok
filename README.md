
  # osoczebialystok.pl

  This is a code bundle for osoczebialystok.pl. The original project is available at https://www.figma.com/design/VJMPRCEG5X2oOUS2BDOeta/osoczebialystok.pl.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  